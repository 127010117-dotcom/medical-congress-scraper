# Project: Congress Abstract Scraper 
- Output all CSVs to /outputs/ folder 
- Column names must match the standard schema (see schema.md) 
- Never truncate text fields; use full content 
- Test every new scraper on 10 rows before full run 
- Use requests + BeautifulSoup unless the site requires JavaScript (use Selenium then)
